node_modules/

# Backups exportados de Supabase — NUNCA se commitean (el repo se sirve publico
# por Cloudflare Pages). Quedan solo como artifact privado del workflow.
backup/

tests/results/*.json
tests/results/*.txt
tests/results/*.html
tests/results/artifacts/
!tests/results/.gitkeep
test-results/
playwright-report/

# Artefactos temporales de prototipado (capturas y scripts de screenshot)
_shot*.js
_shot_*.png

# Mockups de diseño — NO van a produccion (el sitio se sirve publico). Se
# comparten directo con la coach; si un diseño se aprueba, se construye dentro
# del panel real (panel-v2.html), no como archivo suelto.
mockup-*.html
