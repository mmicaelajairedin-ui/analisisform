{
  "name": "pathway-ios",
  "version": "1.0.0",
  "private": true,
  "description": "Wrapper Capacitor de Pathway para la App Store (iOS). Carga el sitio en vivo con ?app=1 para cumplir el modelo reader de Apple (pagos en la web, no en la app).",
  "scripts": {
    "add:ios": "cap add ios",
    "sync": "cap sync",
    "open:ios": "cap open ios",
    "update": "cap update"
  },
  "dependencies": {
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/splash-screen": "^6.0.3",
    "@capacitor/status-bar": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
