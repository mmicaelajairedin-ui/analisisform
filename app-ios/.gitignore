# Dependencias
node_modules/

# Proyectos nativos generados por Capacitor (se regeneran con `npx cap add ios`
# / `npx cap add android`). Se ignoran porque se generan en tu maquina y pesan
# mucho. Si quieres versionar personalizaciones nativas (icono, Info.plist,
# variables.gradle), quita la linea correspondiente y commitea la carpeta.
ios/
android/

# Artefactos de build
*.ipa
*.dSYM.zip
DerivedData/
