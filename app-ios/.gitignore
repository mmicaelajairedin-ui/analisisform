# Dependencias
node_modules/

# Proyecto nativo iOS generado por Capacitor (se regenera con `npx cap add ios`)
# Se ignora porque se genera en la Mac y pesa mucho. Si quieres versionar
# personalizaciones nativas (icono, Info.plist), quita esta linea y commitea ios/.
ios/

# Artefactos de build
*.ipa
*.dSYM.zip
DerivedData/
