diff --git a/panel.html b/panel.html
index 210ca2a..897ebca 100644
--- a/panel.html
+++ b/panel.html
@@ -1147,38 +1147,73 @@ var ME=null;
     return STRIPE_LINK_PRO+'?prefilled_email='+encodeURIComponent((ME&&ME.email)||'');
   };
 
-  // ── Paywall: bloquear acceso si la suscripcion no esta activa ──
-  // activo=false significa que se registraron pero no completaron el pago en
-  // Stripe (o que su suscripcion fue cancelada/vencida). El webhook de Stripe
-  // flipea activo=true cuando la suscripcion entra en trialing/active.
-  // Admin nunca se bloquea (usa la cuenta para soporte/testing).
-  if(ME.rol!=='admin' && ME.activo===false){
-    // Refetch desde Supabase por si el webhook acabo de actualizar pero
-    // localStorage tiene data stale (ej: usuario acaba de pagar, vuelve al panel)
+  // ── Paywall: bloquear acceso si el trial vencio sin suscripcion ──
+  // Modelo trial-sin-tarjeta: la coach usa el panel gratis hasta
+  // `fecha_fin_prueba`. Una vez vencido, si no se suscribio en Stripe
+  // (estado_sub !== 'activa'), ve un paywall full-screen que cubre todo el
+  // contenido. El stripe-webhook flippea estado_sub a 'activa' cuando paga,
+  // asi que se desbloquea solo. Excepciones: admin y es_pro_vitalicio.
+  (function _paywallCheck(){
+    if(ME.rol==='admin') return;
+    var cfg0 = ME.configuracion || {};
+    if(cfg0.es_pro_vitalicio === true) return;
+    if(cfg0.estado_sub === 'activa') return;
+    var fin0 = cfg0.fecha_fin_prueba ? new Date(cfg0.fecha_fin_prueba) : null;
+    // Sin fecha_fin_prueba (cuentas viejas del modelo previo) → no bloqueamos.
+    // Si la cuenta paga, su estado_sub seria 'activa' y ya hubiera retornado.
+    if(!fin0) return;
+    if(fin0.getTime() > Date.now()) return; // trial vigente
+
+    // Trial vencido localmente → refetch para verificar contra DB (por si el
+    // webhook acabo de marcar 'activa' o se extendio el trial).
     fetch(SB+'/rest/v1/usuarios?id=eq.'+encodeURIComponent(ME.id)+'&select=activo,configuracion',{
       headers:{'apikey':KEY,'Authorization':'Bearer '+KEY}
     }).then(function(r){return r.ok?r.json():[];}).then(function(rows){
       var fresh = rows && rows[0];
-      if(fresh && fresh.activo===true){
-        // Webhook acabo de actualizar — refrescar localStorage y recargar panel
-        ME.activo = true;
-        try{ localStorage.setItem('mj_user', JSON.stringify({id:ME.id,email:ME.email,rol:ME.rol,nombre:ME.nombre,activo:true,configuracion:fresh.configuracion||ME.configuracion||{}})); }catch(e){}
+      var freshCfg = (fresh && fresh.configuracion) || cfg0;
+      if(freshCfg.estado_sub === 'activa' || freshCfg.es_pro_vitalicio === true){
+        try{ ME.configuracion = freshCfg; localStorage.setItem('mj_user', JSON.stringify({id:ME.id,email:ME.email,rol:ME.rol,nombre:ME.nombre,activo:(fresh&&fresh.activo!==false),configuracion:freshCfg})); }catch(e){}
         window.location.reload();
         return;
       }
-      // Sigue inactivo — redirect a Stripe Payment Link
-      var fromPopup = fresh && fresh.configuracion && fresh.configuracion.from_popup === true;
-      var stripeUrl = fromPopup
-        ? 'https://buy.stripe.com/9B6eVddqUd4c2YQfa38AE0c'   // 30d trial
-        : 'https://buy.stripe.com/9B6eVddqUd4c2YQfa38AE0c';  // 14d trial
-      // Mensaje breve antes de redirigir
-      document.body.innerHTML = '<div style="position:fixed;inset:0;display:flex;align-items:center;justify-content:center;background:#FAFDF9;font-family:Inter,sans-serif;padding:24px;"><div style="background:#fff;border-radius:14px;padding:32px;max-width:440px;width:100%;text-align:center;box-shadow:0 8px 30px rgba(45,106,79,.1);"><div style="font-size:42px;margin-bottom:14px;">⏸️</div><h1 style="font-family:Fraunces,Georgia,serif;font-size:22px;color:#1B2E26;margin-bottom:10px;">Tu cuenta está pendiente de pago</h1><p style="font-size:14px;color:#5A6A60;line-height:1.55;margin-bottom:20px;">Para acceder al panel, completá la activación de tu suscripción. No cobramos hasta el día '+(fromPopup?30:14)+', cancelás cuando quieras.</p><a href="'+stripeUrl+'?prefilled_email='+encodeURIComponent(ME.email)+'" style="display:inline-block;padding:13px 28px;background:#2D6A4F;color:#fff;border-radius:10px;text-decoration:none;font-size:14px;font-weight:600;margin-bottom:10px;">Completar pago →</a><br><a href="login.html" style="display:inline-block;padding:8px 18px;font-size:12px;color:#5A6A60;text-decoration:underline;">Cerrar sesión</a></div></div>';
+      var freshFin = freshCfg.fecha_fin_prueba ? new Date(freshCfg.fecha_fin_prueba) : null;
+      if(freshFin && freshFin.getTime() > Date.now()) return; // trial extendido
+
+      // Sigue vencido sin suscripcion → mostrar paywall
+      var fromPopup = freshCfg.from_popup === true;
+      var stripeUrl = (fromPopup
+        ? 'https://buy.stripe.com/9B6eVddqUd4c2YQfa38AE0c'   // €58/mes (popup)
+        : 'https://buy.stripe.com/9B6eVddqUd4c2YQfa38AE0c'); // €58/mes (standard)
+      document.body.innerHTML = '<div style="position:fixed;inset:0;display:flex;align-items:center;justify-content:center;background:#FAFDF9;font-family:Inter,sans-serif;padding:24px;"><div style="background:#fff;border-radius:14px;padding:32px;max-width:480px;width:100%;text-align:center;box-shadow:0 8px 30px rgba(45,106,79,.1);"><div style="font-size:42px;margin-bottom:14px;">🌱</div><h1 style="font-family:Fraunces,Georgia,serif;font-size:24px;color:#1B2E26;margin-bottom:10px;letter-spacing:-0.4px;">Tu trial terminó</h1><p style="font-size:14px;color:#5A6A60;line-height:1.55;margin-bottom:20px;">Activá tu suscripción para seguir usando Pathway con todos tus clientes, informes y configuración. <strong>€58/mes, cancelás cuando quieras.</strong></p><a href="'+stripeUrl+'?prefilled_email='+encodeURIComponent(ME.email)+'" style="display:inline-block;padding:13px 28px;background:#2D6A4F;color:#fff;border-radius:10px;text-decoration:none;font-size:14px;font-weight:600;margin-bottom:10px;">Suscribirme →</a><br><a href="login.html" style="display:inline-block;padding:8px 18px;font-size:12px;color:#5A6A60;text-decoration:underline;">Cerrar sesión</a><p style="font-size:11px;color:#999;margin-top:18px;line-height:1.5;">¿Algún problema? Escribinos a <a href="mailto:hi@pathwaycareercoach.com" style="color:#2D6A4F;">hi@pathwaycareercoach.com</a></p></div></div>';
     }).catch(function(){
-      // Si falla el fetch, default a la pantalla de pago
       window.location.href = 'login.html';
     });
-    throw new Error('Pago pendiente');
-  }
+    throw new Error('Trial vencido');
+  })();
+
+  // ── Tracking primer_login + cancelacion del recovery mail ──
+  // Cuando la coach entra al panel por primera vez, guardamos primer_login_at
+  // en su configuracion y BORRAMOS la fila de recovery_5min del email_queue
+  // (asi NO recibe el mail "te ayudo a empezar" si llego sola al panel).
+  (function _markFirstLogin(){
+    if(!ME || !ME.id) return;
+    if(ME.rol !== 'coach' && ME.rol !== 'admin') return;
+    var cfg = ME.configuracion || {};
+    if(cfg.primer_login_at) return; // ya logueada antes
+    var newCfg = Object.assign({}, cfg, { primer_login_at: new Date().toISOString() });
+    fetch(SB+'/rest/v1/usuarios?id=eq.'+encodeURIComponent(ME.id), {
+      method:'PATCH',
+      headers:{apikey:KEY, Authorization:'Bearer '+KEY, 'Content-Type':'application/json', Prefer:'return=minimal'},
+      body: JSON.stringify({ configuracion: newCfg })
+    }).then(function(){
+      try{ ME.configuracion = newCfg; localStorage.setItem('mj_user', JSON.stringify({id:ME.id,email:ME.email,rol:ME.rol,nombre:ME.nombre,activo:ME.activo!==false,configuracion:newCfg})); }catch(e){}
+    }).catch(function(){});
+    // Borrar mail recovery (best-effort, no bloquea nada)
+    fetch(SB+'/rest/v1/email_queue?coach_id=eq.'+encodeURIComponent(ME.id)+'&plantilla_id=eq.recovery_5min', {
+      method:'DELETE',
+      headers:{apikey:KEY, Authorization:'Bearer '+KEY, Prefer:'return=minimal'}
+    }).catch(function(){});
+  })();
 
   var nameEl=document.getElementById('sb-coach-name');
   if(nameEl)nameEl.textContent=ME.nombre||ME.email||'Coach';
