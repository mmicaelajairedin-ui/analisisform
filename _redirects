# Cloudflare Pages redirects
# /coach/<slug> sirve coach.html SIN cambiar la URL (200 = rewrite).
# coach.html lee el slug del pathname, así que no hace falta pasarlo por
# query string (eso evita el placeholder :slug en el query, que es lo que
# puede no estar aplicándose). El splat /coach/* captura cualquier slug.
#
# ⚠️ ORDEN: en _redirects gana la PRIMERA regla que matchea, no la mas
# especifica. Todo 301 de un /coach/<slug> concreto va ARRIBA del splat; si se
# pone debajo, el splat se lo come y el redirect no se ejecuta nunca.
#
# Slugs corregidos (septiembre 2026): dos coaches pegaron la URL ENTERA en el
# campo "slug" del panel y quedo una tira ilegible. Se limpiaron en la base;
# estos 301 mantienen vivos los links que ya hubieran compartido.
/coach/pathwaycareercoachcomcoachpasionfitness504   /coach/pasionfitness504   301
/coach/careercochmariamadrigalcr                    /coach/maria-madrigal     301
/coach/*    /coach.html   200
# /red/<slug> → página pública de la RED (multicoach). red.html lee el slug del pathname.
/red/*      /red.html     200
# /c/<token> → conversación in-app del lead (link mágico). c.html lee el token del pathname.
/c/*        /c.html       200
# /branding → Brand Kit público (URL limpia sin .html para el campo "Branding Resources URL")
/branding   /branding.html   200

# Documentación interna: NO pública. /docs/* contiene borradores y notas
# internas (RGPD, arquitectura) que no deben ser accesibles desde el navegador.
# robots.txt ya los desindexa; esto además los bloquea (404) si alguien adivina
# la URL. Nada del sitio linkea a /docs/.
/docs/*   /404.html   404

# Slugs de coach corregidos (septiembre 2026). Dos coaches pegaron la URL
# ENTERA en el campo "slug" del panel y el sanitizador la dejo como una tira
# ilegible. Se limpiaron en la base; estos 301 mantienen vivos los links que ya
# hubieran compartido. Deben ir ANTES del splat /coach/* de mas arriba? No:
# Cloudflare Pages evalua por especificidad, y una regla exacta gana al splat.
/coach/pathwaycareercoachcomcoachpasionfitness504   /coach/pasionfitness504   301
/coach/careercochmariamadrigalcr                    /coach/maria-madrigal     301

# Redirects 301 de URLs viejas (preservan link equity y evitan 404 en Search Console).
# La landing estaba en landing.html hasta el rename de abril 2026 (ahora es la raíz).
/landing.html   /              301
# panel.html y gestion-leads.html fueron eliminados (reemplazados por panel-v2.html).
/panel.html         /panel-v2.html   301
/gestion-leads.html /panel-v2.html   301
